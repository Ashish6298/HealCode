# Contributing to HealCode

We appreciate contributions to HealCode!

## Development Guidelines
- Always write type hints for all parameters and return types.
- Ensure all source files pass `mypy` strict type checking without errors.
- Write tests under the `tests/` directory for any new scanner or command.
- Maintain full backward compatibility for existing scanners.
